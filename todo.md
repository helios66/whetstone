# todo

Living task list for the Anvil+Dagger → Metro migration. See `changelog.md` for shipped
history and `PLAN_METRO_MIGRATION.md` for the full design.

## In progress
_(none)_

## Open
- [x] FIXED in Mundus 0.10.1: `mundus { enabled = false }` (+ new `-Pmundus.enabled=false` gradle property) now fully disables the compiler — verified bytecode (MainDependency 6→0 refs) AND as a gauntlet scenario. (was: no-op in 0.9.0 — verified at bytecode level (even hardcoded, MainDependency.class keeps all 6 Mundus refs; isApplicable() returns the flag but the compiler instruments regardless). Blocks a clean negative-control test scenario. Once fixed, add a `mundus-disabled` harness scenario asserting compiler slices vanish.
- [x] Overhaul B publishing/CI tier — rewrote `.github/workflows/publish-release.yml` for GitHub Packages (Sonatype→GHP publish tasks, single `RELEASE_TOKEN` PAT for publish+bump, github-actions[bot] identity, dropped DH signing secrets, GHP verify URLs, `permissions: contents/packages: write`). Aligned RELEASING.md (removed drift banner; GHP process + RELEASE_TOKEN scopes: repo/write:packages/read:packages) and scripts/prepare-release.sh (helios66 URLs, GitHub Packages). Publish tasks dry-run-verified. Docs and CI now agree — done 2026-06-13
- [ ] Overhaul B remaining (cosmetic DeliveryHero purge, optional): `settings.gradle.kts` `rootProject.name pd-whetstone→whetstone`; `build-logic` convention plugin id `com.deliveryhero.whetstone.build`→helios66 (internal, non-breaking); README badge. Owner must also provision the `RELEASE_TOKEN` repo secret for CI to run.
- [ ] Overhaul A (package rename `com.deliveryhero.whetstone.*`→helios66) — DEFERRED by owner; revisit only on public release or classpath-coexistence need (touches ~71 files + KSP hardcoded literals + .api baselines + manifest)

## Done
- [x] Dagger-eviction spec + `@Reusable` (2.0.2-SNAPSHOT) — produced the full Dagger→Metro/Whetstone import-mapping (research-verified against Metro 1.2.1: no Reusable/Lazy/Module/Component/BindsInstance types). Classified the consumer's 2,340 Dagger imports: ~1,980 mechanical (SWAP to Metro: @Provides/@Binds/assisted trio/@Reusable; dagger.Lazy→kotlin.Lazy) + ~300 structural (@Component→@DependencyGraph, @Module→@BindingContainer/@ContributesTo, @BindsInstance→@Provides factory param — need restructuring, not import swaps). Closed the one real Metro gap: added no-op `com.deliveryhero.whetstone.Reusable` (Metro tolerates it; verified via runtime test). Spec doc tunnel-served. Open decisions: @Reusable no-op vs @SingleIn, facade depth (typealiases?), structural KSP-assist. Published 2.0.2-SNAPSHOT to GHP — done 2026-06-15
- [x] Anvil-compat `injector.*` contribution annotations for a blocked consumer (3,300 migrated files) — added `@ContributesTo`/`@ContributesBinding`/`@ContributesMultibinding` (Anvil shapes) + KSP translation to native Metro `@ContributesTo` modules. Pivoted from Metro annotation-interop (proven empirically to emit NO cross-module hints) to codegen (the proven cross-module path). Handles boundType (explicit + single-supertype inference), replaces (mapped to the replaced class's generated module — verified fake-wins), set + custom-map-key map multibindings (detects any meta-`@MapKey` annotation, copies it). Verified end-to-end in sample-library via Robolectric (4/0) + 6 new processor unit tests (13/0 total); apiCheck baseline updated. Published 2.0.1-SNAPSHOT to GHP for the consumer — done 2026-06-15
- [x] Mundus capability coverage: multi-dependency + external-library call tracing — (1) added a SECOND Whetstone DI dependency `StatsAuditor` (plain @Inject, no annotations) injected into TodoViewModel; verified Mundus traces TWO distinct DI deps under release+R8 (MainDependency.getMessage + StatsAuditor.audit, plus their Metro factories). Found+fixed a re-entrancy gotcha: calling a traced fn inside a `beginTokenWith {}` metadata lambda drops the enclosing manual span — call it before. (2) Demoed `traceCalleePackages` (0.13.0) tracing CALL SITES into pre-compiled 3rd-party libs okio + gson (added to catalog + sample-library); slices render as `caller → callee` and the readable callee name survives R8 (compile-time literal, pre-obfuscation). Codified all of it: new `validate_deps` in both debug+release, extended the disabled negative-control. Gauntlet green — done 2026-06-14
- [x] Mundus 0.12.0 → 0.13.0 — full gauntlet 85 PASS / 0 FAIL, all 5 captures clean on attempt 1. Surfaced THREE breaking changes: (1) Gradle `-P` rename `composeTracing`→`compose.tracing` (migrated 3 functional + comment mentions); (2) typed extension split `presets.compose`→`composeBodyTracing`+`composeRuntimeTracing` (top-level composeTracing getter removed) — caught loudly at `:build-logic:compileKotlin`, convention updated, `-P` overrides composeRuntimeTracing (verified in plugin bytecode); (3) runtime moved trace output dir `Android/data/$PKG/files/mundus-traces`→`Android/media/$PKG/mundus-traces` (silent; first run failed capture with slices='none' though tracing worked — fixed TDIR). present=true + present=false both build. Kept `-Dmundus.composeTracing` runtime flag as-is — done 2026-06-14
- [x] Guard Mundus behind `-Pmundus.present` (default true) + strip the fixture coupling + harness hygiene (review follow-up) — (1) Mundus plugin is no longer a module `plugins{}` alias; a build-logic convention plugin `com.deliveryhero.whetstone.mundus` applies + typed-configures it, with two source-dir-swapped twins (src/mundus = real, src/noMundus = no-op) picked by the property, so a clone with no GHP access builds via `-Pmundus.present=false` against local runtime stubs in sample-library/src/mundusStub. (2) `TracingFixtures` + its 4 collaborators (AutoTracedDemo/PartlyTracedDemo/ConflictDemo/TracingProbe) now `@Inject constructor`, injected into TodoViewModel via Whetstone instead of `new`. (3) run_variant now `rm -f "$dest"` per attempt so a failed pull can't re-validate a stale host trace. Verified: present=true `:sample:assembleDebug` (210 Mundus refs) + present=false full-app build (stubs, no WireBackend, DI still resolves) both green; debug gauntlet 37/0 PASS — done 2026-06-14
- [x] Merged the two trace scripts into one — added an `e2e` mode to mundus-trace-scenarios.sh (single realistic release journey, flow/lambda on → reviewable trace-e2e-release.perfetto-trace + focused validate_e2e) and deleted the standalone scripts/maestro-trace-e2e.sh. One orchestrator, one Maestro flow, one validation vocabulary. Modes: both (gauntlet/CI gate) | debug|release|disabled|granular|regression | e2e. Verified e2e mode green; both unaffected (guarded additive block) — done 2026-06-14
- [x] Review Finding 2 fix — extracted scripts/_trace-lib.sh (shared adb/pass/fail/assert_ge/assert_eq/q + FAILS); both mundus-trace-scenarios.sh and maestro-trace-e2e.sh now source it instead of copy-pasting the helpers. Syntax + functional + live debug scenario verified (ALL SCENARIOS PASSED) — done 2026-06-14
- [x] Thermo-nuclear review fix — extracted Mundus tracing fixtures out of TodoViewModel into TracingFixtures (exerciseAll + startAndCancel). computeStatsScore 37→20 lines and the displayed stats score is honest again (was polluted by `acc += <fixture>` accumulating 13 fixture return values); 4 scaffolding fields collapsed to 1. Behavior preserved: Maestro gauntlet 86 PASS / 0 FAIL. — done 2026-06-14
- [x] Removed the in-app auto-flow; Maestro now drives ALL tests — deleted TodoHost's scripted LaunchedEffect; seed 2 todos in the ViewModel (initial state) so scoreFor has data + getMessage @TraceArg fires at startup; moved runCancellation() into refreshStats() (fires on Stats visit); added a BackHandler to TodoRoot (so Maestro's `back` pops the nav stack) + the activity-compose dep. Rewrote .maestro/todo-e2e.yaml to drive launch→Stats→Detail. Converted mundus-trace-scenarios.sh run_variant from monkey to `maestro test` (same flow drives every build variant). Confirmed: Maestro-driven gauntlet 86 PASS / 0 FAIL on 0.12.0, and the standalone e2e runner captures + validates a release trace (all surfaces present, lean) — done 2026-06-14
- [x] Mundus 0.11.1 → 0.12.0 (now published on GHP) — full gauntlet: 86 PASS / 0 FAIL, ALL SCENARIOS PASSED, all 4 captures clean on attempt 1. Clean drop-in — done 2026-06-13
- [x] Maestro e2e harness (written, pending Maestro install) — .maestro/todo-e2e.yaml drives the Todo app through a deterministic user journey (add todos, edit detail, stats, toggle); scripts/maestro-trace-e2e.sh builds the RELEASE apk (R8, composeTracing off + flow/lambda on), runs the flow, captures + validates the Mundus trace (DI/suspend/manual span/client compose/ViewModel/@TraceArg). Blocked on installing Maestro (curl|bash denied by auto-mode; brew 'maestro' is a name-collision different tool) — done 2026-06-13
- [x] Doc drift sweep — changelog.md: added the dependency-graph (whetstoneDepGraph/Mermaid) feature to Added, the GitHub Packages distribution (io.github.helios66) to Changed, and a note that sample/sample-library is a Mundus tracing testbed (dev-only). RELEASING.md: added a banner flagging that the fork distributes via GitHub Packages while the Maven Central/Sonatype steps are legacy/unmigrated (Overhaul-B CI rewrite pending). Verified PLAN_METRO_MIGRATION.md still exists; README + dep-graph-spec clean — done 2026-06-13
- [x] Attempted Mundus 0.12.0 — NOT published yet (GHP 404, mavenLocal tops at 0.11.1; plugin marker unresolvable). Reverted to 0.11.1. Caught + fixed a missed traceSuspendFunctions→instrumentSuspendFunctions deprecation in sample-library/build.gradle.kts (sample was migrated in b77c31d, sample-library was missed) — build now clean (no Mundus deprecation warnings) — done 2026-06-13
- [x] T3#5 cancellation span-closure fixture + test — cancellable() (slow traced suspend fn) launched via runCancellation() and cancelled after 60ms; asserts the span CLOSES cleanly (no negative-duration/leaked slice) in debug AND release. Mundus 0.11.1 handles it correctly (emits a cancellable.exception marker; no metadata args on cancellation, minor). Completes Part B coverage. Gauntlet 86 PASS / 0 FAIL — done 2026-06-13
- [x] traceFlowOperators + traceLambdas coverage (opt-in flags, default OFF) — verified both work as intended and tested in BOTH states: default debug/release assert no map#/block# spans (off); new GRANULAR scenario (-Pmundus.traceFlowOperators=true -Pmundus.traceLambdas=true) asserts Flow-operator spans (flowConsumer.map#, 6) + lambda-body spans (<fn>.block#, 26) DO appear. Gauntlet 82 PASS / 0 FAIL — done 2026-06-13
- [x] Mundus 0.11.0 → 0.11.1 + Part B feature tests — probed which Part B items shipped: inline fns now WARN (not silent), structured-concurrency child spans (parallel.async#N nested under parent), exception metadata (error_type + error_message args on .exception slice). Added TracingProbe fixture + validate_partb (debug+release) + check_inline_warning build check. Migrated traceSuspendFunctions→instrumentSuspendFunctions (0.11.1 rename). Gauntlet 76 PASS / 0 FAIL — done 2026-06-13
- [x] Mundus 0.10.2 → 0.11.0 (GitHub Packages) — full gauntlet incl. negative control: 65 PASS / 0 FAIL, ALL SCENARIOS PASSED, all 4 captures clean on attempt 1. Clean drop-in — done 2026-06-13
- [x] Mundus 0.10.1 → 0.10.2 (GitHub Packages) — full gauntlet incl. negative control: 65 PASS / 0 FAIL, ALL SCENARIOS PASSED, all 4 captures clean on attempt 1 (disabled=2 slices, per-scenario floor works). Clean drop-in — done 2026-06-13
- [x] Added negative-control scenario to the gauntlet (-Pmundus.enabled=false -Pmundus.composeTracing=false) — asserts every compiler-injected slice vanishes (getMessage/scoreFor/presets/@AutoTrace/fn-level/@TraceArg/compose all 0) while the manual beginTokenWith span survives and the app still runs. Required fixing run_variant: per-scenario integrity floor (disabled traces are legitimately tiny), case-based task selection, unconditional APK clean. Gauntlet now 65 PASS / 0 FAIL on 0.10.1 — done 2026-06-13
- [x] Mundus 0.9.0 → 0.10.1 (from GitHub Packages) — full gauntlet green: 54 PASS / 0 FAIL, ALL SCENARIOS PASSED. Clean drop-in; all 52 assertions hold (presets, @TraceArg, @AutoTrace/@NoTrace + precedence, thread attribution, span balance, multi-module, debug-heavy/release-lean, R8 full-mode). Capture-retry hardening absorbed a corrupt debug trace (retried, OK on attempt 2) — done 2026-06-13
- [x] @NoTrace-class + @AutoTrace-fn precedence test + harness hardening — observed @NoTrace(class) overrides @AutoTrace(fn) (contested untraced, no compiler warning); pinned as a contract assertion. Hardened run_variant: (1) fail loudly on build error instead of silently using a stale APK [caught a real false-result], (2) clean the release APK so flagged/unflagged release builds can't collide, (3) integrity-checked capture retry (re-run flow up to 3x if the pulled trace is corrupt/tiny) instead of asserting on garbage. Suite 52 assertions, all green — done 2026-06-13
- [x] Function-level @AutoTrace / @NoTrace permutations — PartlyTracedDemo (class NOT annotated, outside includePackages): @AutoTrace on tracedOne traces, plainTwo sibling does not; @NoTrace on MainDependency.silentHelper (IN includePackages) stays untraced while getMessage sibling traces. Verified bytecode (tracedOne 8 refs / plainTwo 0; getMessage 6 / silentHelper 0) AND runtime in debug+release. Suite now 51 assertions, all green — done 2026-06-13
- [x] @AutoTrace / @NoTrace annotation coverage (answered "do we test the autotrace annotation w/ + w/o coroutines?" — we didn't, only package-based includePackages). Added AutoTracedDemo fixture in com.example.mundusdemo (OUTSIDE includePackages, so @AutoTrace is the sole cause) with a plain method, a suspend method, and a @NoTrace method; wired into TodoViewModel background work. Harness validate_autotrace asserts @AutoTrace traces both coroutine (weighAsync) + non-coroutine (weigh) and @NoTrace stays untraced — pass in debug AND release (survives R8). Suite now 45 assertions — done 2026-06-13
- [x] Tier 1 harness expansion (robust Android suite, 19→39 assertions, all pass on 0.9.0 via GHP) — per-preset coverage (lifecycle/startupPhases/viewModel/workers each fire, FQN-anchored), thread attribution (scoreFor on DefaultDispatcher, NOT main), span balance (manual statsBatch spans closed + <2% unclosed overall), multi-module reach (:sample + :sample-library). Run in BOTH debug and release so each is also verified to survive R8 full mode — done 2026-06-13
- [x] Move Mundus off mavenLocal → private GitHub Packages (maven.pkg.github.com/helios66/mundus) in settings.gradle.kts (both pluginManagement + dependencyResolutionManagement, gpr.user/gpr.key creds). Verified: purged the mundus gradle cache to force a real GHP fetch, harness 19/19 pass end-to-end (plugin + runtime + compose-tracing all resolved from GHP) — done 2026-06-13
- [x] Add regression-guard scenarios to mundus-trace-scenarios.sh — (1) metadata VALUE correctness (debug.filter=='all', debug.todoCount sane int — guards typed-put corruption/column-swap); (2) composeTracing causality (release-default WITHOUT flag must be heavy: 10321 vs flagged 0 — proves the flag, not R8, causes lean; guards false-lean + 0.6.0 auto-install regression). 19/19 assertions pass on 0.9.0. Found that mundus enabled=false is a no-op (see Open) — done 2026-06-13
- [x] Mundus 0.8.0 → 0.9.0 bump — ran scripts/mundus-trace-scenarios.sh both; all 16 invariants pass (debug 9370 compose events heavy; release 0 lean under R8 full mode; @TraceArg + beginTokenWith + Whetstone DI all survive). Behaves identically to 0.8.0 — done 2026-06-13
- [x] Mundus 0.7.0 → 0.8.0 bump + codified scenario harness (scripts/mundus-trace-scenarios.sh) — drives debug+release flows on the emulator, extracts the Perfetto trace, validates 16 invariants (debug heavy 5859 compose events; release lean 0 under R8 full mode; getMessage/scoreFor/statsBatch + @TraceArg debug.title/debug.todo + beginTokenWith debug.todoCount-int/debug.filter-string survive R8). All pass on 0.8.0 — done 2026-06-13
- [x] Merge local/private-publish into main (4aec94e) — wires private GitHub Packages publishing: library modules + gradle plugin + plugin-marker publish to helios66/whetstone-private; POM/flags repointed off DeliveryHero (mavenCentral/sign=false, POM_URL/SCM/DEVELOPER→helios66, all under whetstone-private). GHP publish tasks verified to materialize. Pushed to fork/main — done 2026-06-13
- [x] R8 full mode made explicit (`android.enableR8.fullMode=true`; was already AGP-8 default) + release re-verified on emulator — Whetstone/Metro DI survives (App/ViewModel(168 slices)/Worker injection all resolve, no lateinit/ClassNotFound/Metro errors); Mundus survives (491 slices, 0 compose with flag off; `@TraceArg` debug.title×19/debug.todo×6 across the suspend hop + `beginTokenWith` debug.todoCount=3 int/debug.filter=all string land in correct Perfetto columns) — done 2026-06-13
- [x] Mundus 0.6.0 → 0.7.0 bump + emulator trace test — 0.7.0 ships the requested opt-out: `mundus { composeTracing }` build flag (default true; `-Pmundus.composeTracing=false` drops the compose-tracing dep) + runtime kill switch `-Dmundus.composeTracing=false`. Replaced the `configurations…exclude` hack with the official flag. Verified debug 7630 compose events vs release (flag off) 0; `@TraceArg`/`beginTokenWith` metadata survive R8 in both — done 2026-06-13
- [x] Whetstone dependency-graph (Mermaid) feature — Phase 1 (per-module) + Phase 2 (whole-app aggregation) + renderer unit tests; verified on :sample (7 scopes/8 contributions) and :sample-library — done 2026-06-13 (see docs/dep-graph-spec.md)
- [x] Stage 9: emulator parity run on Pixel 6a — Application/ViewModel(Compose + delegate)/View/Service/Worker injection all verified working; nested App→Activity→View graph extensions resolve; no crashes — done 2026-06-12
- [x] Stage 10: configuration-cache-test updated — drops removed-proguard test; adds 2 codegen tests asserting the graph + injector + instance contribution shapes; worker LoadClassTest retained; all pass — done 2026-06-12
- [x] Stage 11: README updated to Metro; binary-compat baseline regenerated (metro.hints excluded); dropped unused anvil/dagger/kapt/autoService catalog entries; full `./gradlew build` green — done 2026-06-12
- [x] Stage 1: version catalog — Kotlin 2.2.0→2.3.20, add Metro 1.2.0 + KSP 2.3.9 + kotlinpoet 2.3.0 — done 2026-06-12
- [x] Stage 2: runtime annotations + scopes (SingleIn/ForScope typealias to Metro; meta annotations kept) — done 2026-06-12
- [x] Stage 4: whetstone runtime — graphs/extensions/multibinding factories on Metro; :whetstone green — done 2026-06-12
- [x] Stage 5: whetstone-compose on Metro (source unchanged) — done 2026-06-12
- [x] Stage 6: whetstone-worker on Metro — done 2026-06-12
- [x] Stage 7: gradle plugin rewrite (metro+ksp, javax interop, eager ksp dep, drop anvil/proguard) — done 2026-06-12
- [x] Stage 3: whetstone-compiler → KSP SymbolProcessor — done 2026-06-12
- [x] Stage 8: samples build green — sample app DI graph assembles end-to-end on Metro; APK built — done 2026-06-12
- [x] Stage 0: baseline build green + Metro pattern spike (4/4 patterns validated: member-injector multibinding, graph extension + contributed factory, instance multibinding via `() -> Base`, **KSP-generated `@ContributesTo` merged by Metro**) — done 2026-06-12. Version trio locked: Kotlin 2.3.20 + Metro 1.2.0 + KSP 2.3.9.
