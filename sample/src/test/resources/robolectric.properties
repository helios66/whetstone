# Run Robolectric against SDK 35: the project compiles against SDK 36, but Robolectric only
# supports SDK 36 on Java 21, while the unit-test toolchain is Java 17. The runtime DI assertions
# here are API-version-agnostic, so SDK 35 (which runs on Java 17) is sufficient.
sdk=35
